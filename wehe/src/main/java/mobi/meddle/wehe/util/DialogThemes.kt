package mobi.meddle.wehe.util

/**
 * The theme that the deprecated `AlertDialog.THEME_DEVICE_DEFAULT_LIGHT` constant resolves to
 * internally. Passing the style directly to `AlertDialog.Builder(context, themeResId)` keeps the
 * dialogs looking exactly the same without using the deprecated constant.
 */
const val THEME_DEVICE_DEFAULT_LIGHT_DIALOG = android.R.style.Theme_DeviceDefault_Light_Dialog_Alert
